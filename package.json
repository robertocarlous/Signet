{
  "name": "signet",
  "packageManager": "pnpm@11.5.0",
  "version": "2.0.0-0",
  "author": "Andrew Miracle <andrew@daccred.co>",
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.6",
    "@stellar/stellar-sdk": "^16.3.0",
    "conventional-changelog-conventionalcommits": "^8.0.0",
    "semantic-release": "^24.2.9"
  },
  "keywords": [
    "attest",
    "attestation-service",
    "monorepo",
    "pnpm",
    "soroban",
    "stellar"
  ],
  "private": "true",
  "repository": "daccred/signet",
  "scripts": {
    "build": "pnpm run --recursive build",
    "build:docs": "pnpm --filter @signetprotocol/docs build",
    "changeset": "changeset",
    "commit": "cz",
    "dev": "pnpm run --recursive dev",
    "dev:docs": "pnpm --filter @signetprotocol/docs dev",
    "filter": "pnpm --filter",
    "format": "pnpm run -r format",
    "lint": "pnpm run -r lint",
    "release": "pnpm release:version && npx release-it@17.11.0",
    "release:stellar": "cd contracts/stellar && cargo release $1 --execute",
    "release:version": "pnpm changeset && pnpm changeset version",
    "semantic-release": "semantic-release",
    "syncpack": "npx syncpack",
    "syncpack:format": "npx syncpack format && npx syncpack list",
    "test": "pnpm run -r test"
  },
  "workspaces": [
    "packages/*",
    "contracts/*"
  ]
}
